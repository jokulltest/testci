name: Label Trigger Workflow

on:
  pull_request: {}

jobs:
  handle-label:
    runs-on: ubuntu-latest
    steps:
      - name: Check Label Name
        run: |
          echo "full_name: ${{ github.event.action }}"
